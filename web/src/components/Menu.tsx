import styled from "styled-components";

export const Menu = styled.div`
    position: absolute;
    display: flex;
    flex-direction: row;
    
    z-index: 1000;

    justify-content: space-evenly;

    top: 20px;
    right: 20px;

`